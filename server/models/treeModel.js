const Tree = {
  initialCost: 120, // Initial cost per tree ($)
  annualCostPercentage: 10, // Annual cost as a percentage of the initial cost
  co2OffsetPerYear: 5.7, // CO2 offset per tree per year (kg)
  fullyGrownYear: 6 // Year when the tree becomes fully grown
};

module.exports = Tree;
